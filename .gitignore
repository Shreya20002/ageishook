# node / next
node_modules
.next
next-dev*.log

# foundry / hardhat build output
cache
out
artifacts
coverage
typechain-types

# secrets — never commit these
.env
.env.*
!.env.example

# local anvil deploy records (real testnet broadcasts are kept as deployment evidence)
broadcast/*/31337/
broadcast/*/*/dry-run/
